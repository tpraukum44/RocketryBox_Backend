# NPM configuration for production deployment
registry=https://registry.npmjs.org/
# Handle behind corporate proxies
strict-ssl=true
# Prevent optional dependencies from failing the build
optional=false
# Ensure latest package-lock.json format
package-lock=true 