# Node.js environment configuration for Elastic Beanstalk
# Node.js version is determined by package.json engines and .nvmrc
# Environment variables are consolidated in platform.config to avoid duplicates 